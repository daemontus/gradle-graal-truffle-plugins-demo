import com.oracle.truffle.gradle.NativeImage

apply plugin: 'application'
apply plugin: 'org.graalvm.plugin.native-image'

repositories { jcenter() }
application { mainClassName 'demo.App' }

task customBinary(type: NativeImage) {
    outputName 'my-awesome-binary'
    forMainClass 'demo.App'
}